info:
  name: login
  type: http
  seq: 7

http:
  method: POST
  url: https://kitek.ktkv.dev/marketplace/api/auth/register
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
