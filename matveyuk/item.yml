info:
  name: item
  type: http
  seq: 12

http:
  method: POST
  url: https://kitek.ktkv.dev/marketplace/api/items
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
