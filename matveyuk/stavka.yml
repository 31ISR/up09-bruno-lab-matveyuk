info:
  name: stavka
  type: http
  seq: 14

http:
  method: POST
  url: https://kitek.ktkv.dev/marketplace/api/items/:id/bids
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
